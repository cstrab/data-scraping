import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders time range label', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/time range/i);
//   expect(linkElement).toBeInTheDocument();
// });
